// jest-dom matchers (toBeInTheDocument, …) for vitest's expect — loaded at
// runtime by vitest.setup.ts; this file brings the type augmentation into
// the app tsconfig so `tsc -b` typechecks test files with them.
import '@testing-library/jest-dom/vitest';
